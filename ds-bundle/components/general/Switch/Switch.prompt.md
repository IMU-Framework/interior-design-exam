Switch from repo. Use via `window.InteriorDesignExam.Switch` (bundle loaded from the root `_ds_bundle.js`).

Toggle switch — binary on/off control.
@param {{ on: boolean, onChange: (next: boolean) => void }} props
