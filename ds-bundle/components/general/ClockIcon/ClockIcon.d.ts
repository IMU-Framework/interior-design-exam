import * as React from 'react';

/**
 * ClockIcon — from repo@1.0.0.
 */
export interface ClockIconProps {
  [key: string]: unknown;
}

export declare const ClockIcon: React.ComponentType<ClockIconProps>;
